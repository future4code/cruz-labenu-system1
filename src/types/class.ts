export const valid_modules= [0, 1, 2, 3, 4, 5, 6, 7]

type classType = {
    id: string, 
    name: string,
    start_date: Date,
    end_date: Date, 
    current_module: string
}

export default classType;